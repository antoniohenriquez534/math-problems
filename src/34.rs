fn main() {
    // Define variables
    let a = 5;
    let b = 3;

    // Perform arithmetic operations
    let result = a + b;

    // Print the result
    println!("The sum of {} and {} is {}", a, b, result);
}
